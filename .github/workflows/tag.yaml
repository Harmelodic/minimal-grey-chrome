# $schema: http://json.schemastore.org/github-workflow.json
name: tag

on:
  push:
    tags:
      - "*"
  workflow_dispatch: {}

jobs:
  release-browser-extension:
    uses: harmelodic/workflows/.github/workflows/release-browser-extension.yaml@9c9425790780e8e5a34289a409f080f2b413affd
    with:
      extension-name: minimal-grey-chrome
      release-version: ${{ github.ref_name }}
